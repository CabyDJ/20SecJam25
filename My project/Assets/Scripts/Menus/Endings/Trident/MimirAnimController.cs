using UnityEngine;

public class MimirAnimController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //DO THE THINGS
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
